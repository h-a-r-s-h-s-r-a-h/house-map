(defun ch (m / n)
        (setq condition m)
    	(if (= condition m)
	  (setq n 15)
	)
       (if (= condition 7)

         (setq condition 5)
	 )
n
  )
(defun valuek ()

   (setq l (ch 0))
  (princ l)
  )
   
   